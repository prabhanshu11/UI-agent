[project]
name = "ui-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mss>=10.1.0",
    "pynput>=1.8.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
